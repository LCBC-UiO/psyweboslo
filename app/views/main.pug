extends scaffold

block content
    div(class="cover-container d-flex h-100 p-3 mx-auto flex-column mt-5")
      header(class="masthead mb-auto")
      main(role="main" class="inner cover")
        h1(class="cover-heading mb-5") PsyWeb Oslo
        p(class="lead") Joint infrastructure to run psychological, web-based experiments for MoBa (FHI) and PSI.
        p(class="lead")
          a(href="exp_list" class="btn btn-lg btn-primary mt-3 mb-5") Start Experiment
  
      footer(class="mastfoot mt-auto")
        div(class="inner")
          p